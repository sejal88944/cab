<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme (Material 3) -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/rideeasy_primary</item>
        <item name="colorPrimaryDark">@color/rideeasy_primary_dark</item>
        <item name="colorOnPrimary">@color/rideeasy_on_primary</item>
        <item name="android:statusBarColor">@color/rideeasy_surface</item>
        <item name="android:navigationBarColor">@color/rideeasy_surface</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/rideeasy_surface</item>
        <item name="android:navigationBarColor">@color/rideeasy_surface</item>
    </style>

    <!-- Launch: splash screen (core-splashscreen) -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@drawable/splash</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/rideeasy_primary</item>
    </style>
</resources>